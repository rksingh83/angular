import { CatchEventDirective } from './catch-event.directive';

describe('CatchEventDirective', () => {
  it('should create an instance', () => {
    const directive = new CatchEventDirective();
    expect(directive).toBeTruthy();
  });
});
